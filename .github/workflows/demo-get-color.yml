name: Run Get Color Demo

on: [repository_dispatch]

jobs:
  demo-get-color:
    if: github.event.action == 'get_color'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Deploy Demo Workflow "Get Color"
        uses: jwulf/zeebe-action@master
        with:
          client_config: ${{ secrets.ZEEBE_CLIENT_CONFIG }}
          operation: deployWorkflow
          bpmn_filename: bpmn/demo-get-color.bpmn
      - name: Generate Random Number
        id: random
        uses: jwulf/random-number@master
      - name: Execute Demo Workflow "Get Color"
        id: color
        uses: jwulf/zeebe-action@master
        with:
          operation: createWorkflowInstanceWithResult
          bpmn_process_id: demo-get-color
          variables: '{"id": ${{steps.random.outputs.number}} }'
      - name: Print Output
        run: echo "The random color is ${{ steps.color.outputs.result.data.name }}"
